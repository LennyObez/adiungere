{
  "language": "en",
  "strings": {
    "facts.only": "This report states facts about bytes. It does not decide whether a recording was altered before it was seen, and no line in it should be read as such a decision.",
    "not.an.examination": "This is not an authentication examination.",
    "inspection.moov.first": "The movie box precedes the media data, which is how the reference camera writes its files.",
    "inspection.moov.last": "The movie box follows the media data. The reference camera writes it first; a file from that camera laid out this way passed through another program. Other recorders write this layout themselves.",
    "inspection.moov.none": "The file has no media data box.",
    "inspection.tail": "The file ends with {bytes} byte(s) that do not read as a box or belong to a box declaring more than the file holds. A recorder that pre-allocates its files leaves such a tail, and so does one that lost power.",
    "inspection.tracks": "{count} track(s): {list}.",
    "inspection.vendor.present": "{count} vendor box(es) preserved as bytes, not interpreted: {list}.",
    "inspection.vendor.absent": "No vendor box is present. A recording from the reference camera carries two; a file from that camera without them passed through another program. Other recorders write none.",
    "inspection.user.data.standard": "{count} user-data box(es) of a type the file format defines, preserved as bytes, not interpreted: {list}. A common muxer writes such a box; it is not a recorder's.",
    "inspection.default.tracks": "{count} video tracks are marked as the default, which is why players disagree about which one to show.",
    "integrity.file.identical": "The file is identical, byte for byte, to the one the manifest was made from.",
    "integrity.file.differs": "The file differs from the one the manifest was made from: recorded {recorded}, observed {observed}.",
    "integrity.file.not.recorded": "The manifest records no whole-file digest, so the file as a whole was not compared.",
    "integrity.track.identical": "Track {index}: the stored samples are identical to those the manifest records.",
    "integrity.track.differs": "Track {index}: the stored samples differ from those the manifest records.",
    "integrity.track.not.recorded": "Track {index}: the manifest records no fingerprint, so the samples were not compared.",
    "integrity.track.not.checkable": "Track {index}: the samples could not be compared: {reason}.",
    "integrity.track.missing": "Track {index}: the manifest records a track the file does not have.",
    "integrity.vendor.identical": "Vendor box {kind} at {location}: identical to the bytes the manifest records.",
    "integrity.vendor.differs": "Vendor box {kind} at {location}: differs from the bytes the manifest records.",
    "integrity.vendor.absent": "Vendor box {kind} at {location}: recorded in the manifest and absent from the file.",
    "integrity.vendor.unexpected": "Vendor box {kind} at {location}: present in the file and not recorded in the manifest.",
    "integrity.structure.identical": "The container structure matches the one the manifest records.",
    "integrity.structure.differs": "The container structure differs from the one the manifest records.",
    "structure.rewrite.none": "No sign of rewriting was found in the container structure. This is not an authentication examination.",
    "structure.rewrite.some": "The container structure shows {count} sign(s) that another program wrote this file:",
    "structure.sign.moov.after.mdat": "the movie box follows the media data",
    "structure.sign.encoder.tag": "the encoder tag {tag} belongs to another program",
    "structure.sign.single.video": "one video track where a sibling recording has two",
    "structure.sign.vendor.absent": "the vendor boxes a recording from this camera carries are absent",
    "structure.sign.resolution.capped": "the resolution {width} by {height} is below what this camera records",
    "structure.unknown.layout": "The file does not follow a naming grammar this product knows, so no comparison with a camera's layout was made.",
    "time.no.later.than": "By {clock}, this recording dates from no later than {value}.",
    "time.container.clock.note": "The container clock is written by the recorder into a field defined as UTC; the reference recorder writes local time there, and consecutive files share one value, so it is a session clock rather than a capture time.",
    "time.file.name.note": "The file name carries a local time with no zone, written by the recorder at the start of the file.",
    "time.satellite.clock.note": "The satellite clock is UTC from the receiver, one sentence per second.",
    "time.system.clock.note": "The system clock of the machine that produced this manifest, read as UTC.",
    "time.disagreement": "The clocks disagree: {list}. None of them is taken as the capture time.",
    "fingerprint.track": "Track {index} fingerprint ({version}): {digest} over {count} samples and {bytes} bytes, prefix width {width}.",
    "fingerprint.stream": "Track {index} elementary stream ({rule}): {digest} over {bytes} bytes, reproducible with the documented command.",
    "fingerprint.file": "Whole file: {digest} over {bytes} bytes, reproducible with a digest utility.",
    "fingerprint.structure": "Structure: {digest} over {count} observations.",
    "scan.single.dual": "{name}: one file holding two cameras.",
    "scan.single.mono": "{name}: one file holding one camera.",
    "scan.paired": "{front} and {rear}: two files, one per camera, matched by name.",
    "scan.unpaired": "{name}: named as one camera of a pair; its counterpart was not found.",
    "scan.not.recognised": "{name}: not recognised as a dashcam recording by name or by structure.",
    "scan.not.readable": "{name}: could not be read: {reason}.",
    "scan.skipped": "{count} file(s) were not opened, because their extension is not one a recording carries."
  }
}
