# Every path has an owner. The paths listed after the catch-all are the ones where a change alters what the
# product promises rather than what it does: the guarantee suite, the decision records, the evidence
# register, the pipelines and the deployment descriptions. The format reader and the provenance pipeline
# join this list with the milestones that create them.

*                                       @LennyObez

/core/crates/adiungere-guarantees/      @LennyObez
/docs/adr/                              @LennyObez
/docs/evidence.md                       @LennyObez
/.github/workflows/                     @LennyObez
/infra/                                 @LennyObez
